name: sample-ci-ignore-optional-analysis
on:
  push:
    paths-ignore:
      - 'phase1_optional/**'
      - 'rna_velocity_pseudotime_analysis/**'
  pull_request:
    paths-ignore:
      - 'phase1_optional/**'
      - 'rna_velocity_pseudotime_analysis/**'
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "optional analysis modules excluded from CI scope"
